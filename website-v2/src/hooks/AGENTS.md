# AGENTS.md

- `useMobileDrawer.ts` - nav drawer state
- `useProjectFilter.ts` - project filtering
- `useToast.ts` - toast state

Scope is hooks only. For `services/`, `config/`, `data/`, `types/`, `styles/` see their own `AGENTS.md`.
