# AGENTS.md

- `ContactForm.tsx` - form UI (uses `services/contact.ts` + `hooks/useToast`)
- `ContactSide.tsx` - contact details / side panel (reads `data/contact.ts`)
- `Contact.css` - shared contact styles
