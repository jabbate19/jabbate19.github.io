# AGENTS.md

- `Nav.tsx` / `Nav.css` - top navigation with mobile drawer (uses `hooks/useMobileDrawer`)
- `Footer.tsx` / `Footer.css` - site footer
- `PageTransition.tsx` - route transition wrapper (uses `motion`)
